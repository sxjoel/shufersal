<div>
  <div class="container" style="margin-top: 100px;">
    <table class="table" >
      <thead>
      <tr>
        <th>שם המוצר</th>
        <th>מחיר</th>
        <th>מזמינים</th>
        <th>כמות</th>
        <th>תמונה</th>
      </tr>
      </thead>

      <% @products.each do |product| %>
          <tr>
            <td><%= product['product_name'] %></td>
            <td><%= product['price'] %></td>
            <td><%= product['user_names'].join(",") %></td>
            <td><%= product['quantity'] %></td>
            <td><img src="<%= product['img'] %>"></td>
          </tr>
      <% end %>
    </table>
  </div>


</div>
